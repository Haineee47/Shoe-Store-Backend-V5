/**
 * Provides shared repository abstractions for the persistence layer.
 *
 * <p>This package contains reusable repository contracts and infrastructure that can be shared
 * across application modules.
 *
 * <p>Business-specific repositories (for example, UserRepository, ProductRepository,
 * OrderRepository) must remain inside their owning modules and must not be placed in this package.
 */
package com.shoestore.shared.persistence.repository;
